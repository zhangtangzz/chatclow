ChatClow 项目更改日志
====================

2026-06-06 功能缺口补齐（6项）
------------------------------

【新增功能】
1. 会话切换时加载历史消息
2. 删除会话（含级联删除聊天记录）
3. 清除对话记忆（保留会话，清空聊天记录）
4. 模型参数可配置：temperature / maxTokens / topP
5. 管理员查看用户对话详情弹窗
6. 记忆开关真正生效（关闭后AI不带历史上下文）

【后端改动】
- ChatRequest.java          → 新增 memoryEnabled 字段
- ChatContext.java          → 新增 memoryEnabled 字段 + getter/setter
- ChatService.java          → 接口加 memoryEnabled 参数
- ChatServiceImpl.java      → 传递 memoryEnabled 到 ChatContext
- ChatController.java       → 从请求读取 memoryEnabled 传给 Service
- ContextAssemblyStep.java  → memoryEnabled=false 时跳过加载历史
- AgentConversationService.java     → 新增 deleteConversation / clearMemory 方法
- AgentConversationServiceImpl.java → 实现删除会话 + 清除记忆
- AgentConversationController.java  → 新增 DELETE /delete/{id} 和 /memory/{convId}
- AiModel.java              → 新增 temperature / maxTokens / topP 字段
- ModelCallStep.java        → 请求体加模型参数
- SyncAiCallStep.java       → 第一次+第二次请求加模型参数
- FunctionCallingStep.java  → 第二次请求加模型参数
- UserController.java       → adminGetUserConversations 真正实现

【前端改动】
- api/chat.js       → 新增 getConversationRecords / deleteConversation / clearConversationMemory
                       chatStream body 加 memoryEnabled
- api/token.js      → 新增（上轮）getUserTokenStats / getAdminTokenSummary
- views/Chat.vue    → 切换会话加载历史 / 悬停删除按钮 / 清除记忆对接API
- views/Admin.vue   → 模型表单加温度/Token/Top-P / 对话详情弹窗
- views/TokenStats.vue → 新增用量统计页面（上轮）
- router/index.js   → 新增 /token-stats 路由（上轮）

【数据库变更】
- chatclow_ai_conversation.total_tokens  INT DEFAULT 0
- chatclow_ai_model.temperature          DOUBLE DEFAULT 0.7
- chatclow_ai_model.max_tokens           INT DEFAULT 4096
- chatclow_ai_model.top_p                DOUBLE DEFAULT 1.0

2026-06-06 测试体系搭建（三层 51 个用例全部通过）
-------------------------------------------------

【测试基础设施】
- pom.xml                             → 新增 H2 依赖（test scope）
- src/test/resources/application-test.yml → H2 测试环境配置（MySQL 兼容模式）
- src/test/resources/schema-h2.sql    → H2 建表 DDL（9 张表）

【Tier 1：Step 单元测试（Mockito，29 个）】
- ContextAssemblyStepTest.java  (9 tests) → Agent/Model 加载、会话创建/加载、记忆开关、历史消息、异常场景
- MessageBuildStepTest.java     (7 tests) → 消息构建、RAG 上下文拼接、工具 JSON、消息顺序
- RecordSaveStepTest.java       (7 tests) → 回复保存、token 累计、边界条件
- ToolCallHelperTest.java       (6 tests) → FC 消息拼接、推理内容、多工具调用、参数解析

【Tier 2：Service 集成测试（H2，15 个）】
- AgentConversationServiceImplTest.java      (5 tests) → 创建/查询/删除会话、用户隔离、清除记忆
- AgentConversationRecordServiceImplTest.java (3 tests) → 保存/查询记录、空数据
- ChatServiceImplTest.java                   (2 tests) → 非流式/流式链路、ChatContext 构建
- RagDocumentServiceImplTest.java            (5 tests) → 文档 CRUD

【Tier 3：Controller 接口测试（MockMvc，7 个）】
- LoginControllerTest.java   (3 tests) → 登录成功、密码错误、用户不存在
- TokenControllerTest.java   (2 tests) → 用户 token 统计、管理员汇总排序
- ChatControllerTest.java    (2 tests) → /send 返回结构、/send-stream SSE

2026-06-06 SSE 流式输出修复（多轮迭代，最终方案）
-----------------------------------------------

【问题】前端 AI 回复不是逐字流式出现，全文一次性显示
【排查过程】
  1. 后端 Tomcat response.setBufferSize(0) → F12 确认 SSE 已流式 ✅
  2. fetch ReadableStream + async 回调 + setTimeout → 仍不行 ❌
  3. setInterval 缓冲队列 → 引入响应式 bug，逻辑错乱 ❌
  4. await + setTimeout(30ms) → 仍不行 ❌
【根本原因】Vue 3 响应式：原始对象 aiMsg.content += text 绕过 Proxy，Vue 检测不到变化不更新 DOM
【最终方案：requestAnimationFrame 逐字渲染】
  - ChatController.java → sendStream() 加 HttpServletResponse，response.setBufferSize(0)
  - api/chat.js         → 回调同步执行（无 await），速度快
  - views/Chat.vue      → 新增 pendingChars 缓冲 + RAF 渲染循环
    - SSE content 事件 → pendingChars += event.data（快速收集）
    - renderNextChar()  → 每帧取 2 个字符写入 messages.value[idx].content（响应式代理）
    - done/error        → stopCharRender() 清空缓冲 + 取消 RAF
    架构：SSE 只管收字，RAF 独立驱动渲染，两者完全解耦

2026-06-06 03:27:50 更新全局约束
------------------------------
- CLAUDE.md → 日志规则改为精确到秒钟，同一问题反复修改也要每次都记录

2026-06-06 03:45:00 SSE 流式前端重写（第6次，独立 ref + setInterval 方案）
-----------------------------------------------------------------------
【改动方案】
- 根本原因：之前 RAF 方案仍可能因 Vue 响应式代理问题导致 msg.content += text 不触发 DOM 更新
- 新方案：用独立的 ref（streamText）驱动渲染，与消息对象完全解耦
  1. SSE content 事件 → rawBuffer（非响应式字符串，最快速度收集）
  2. setInterval 每 30ms → 从 rawBuffer 取 3 个字符写入 streamText.value（触发响应式）
  3. 模板 displayContent(msg, idx) → msg.content + streamText.value（响应式绑定）
  4. done/error → commitStreamText() 将 streamText 提交到 msg.content 并重置
【前端改动】
- views/Chat.vue → 变量：streamText ref + rawBuffer + streamTimer 替代 pendingChars + RAF
- views/Chat.vue → 函数：displayContent / startStreamRender / stopStreamRender / commitStreamText
- views/Chat.vue → 模板：v-html 用 displayContent(msg, idx)，打字指示器 v-if="streaming && !streamText"
- views/Chat.vue → sendMessage：content 事件写 rawBuffer，done/error 调 commitStreamText

2026-06-06 04:15:00 上线前功能补齐（BCrypt + 注册 + JWT刷新 + 限流 + 生产配置 + 错误边界）
-------------------------------------------------------------------------------
【改动清单】
1. BCrypt 密码加密
   - pom.xml → 加 spring-security-crypto 5.7.6
   - UserServiceImpl.java → register() 中 encode 密码
   - LoginController.java → matches() 校验密码
   - UserController.java → adminUpdateUser 判断密码是否需加密

2. 前端注册页面
   - api/auth.js → 新增 register() 接口
   - views/Login.vue → 新增注册/登录切换模式
   - 注册表单：用户名 + 密码 + 确认密码 + 邮箱 + 表单验证规则
   - 注册成功自动切回登录

3. JWT 刷新机制
   - JwtUtil.java → 新增 generateRefreshToken()（7天有效期）
   - LoginController.java → 登录返回 refreshToken + 新增 POST /api/auth/refresh
   - WebMvcConfig.java → /api/auth/refresh 排除 JWT 拦截
   - api/request.js → 响应拦截器 401 自动用 refreshToken 刷新后重试
   - stores/user.js → 保存 refreshToken 到 localStorage

4. 请求限流
   - config/RateLimitingFilter.java → 基于 IP+路径 滑动窗口限流
   - 对话接口 30次/分钟，其余 API 60次/分钟

5. 生产环境配置
   - application-prod.yml → 数据库/Redis/Mongo/PG 密钥通过环境变量注入

6. 前端错误边界
   - components/ErrorBoundary.vue → 渲染错误兜底组件
   - App.vue → 包裹 router-view
   - main.js → 全局 errorHandler + unhandledrejection 监听

2026-06-06 04:30:00 修复 BCrypt 兼容性 + 完整上线前审计
--------------------------------------------------------
【修复】
- LoginController.java → 兼容旧版明文密码（检测是否以 $2a$/$2b$ 开头）
- LoginControllerTest.java → 测试用户密码改为 BCrypt 编码
- UserServiceImpl.java → 修复重复 import

【审计结论】核心链路完整，51 测试全部通过。以下为不阻塞上线但建议后续迭代补齐的功能：
  1. 404 页面（访问不存在路由显示空白）
  2. 用户密码修改
  3. 忘记密码/重置密码
  4. 消息复制
  5. 会话搜索/筛选
  6. 知识库文档预览
  7. 移动端响应式适配（Chat.vue）

2026-06-06 04:45:00 确定 UI 设计风格为 Handwritten Sketch（手绘白板笔记）
------------------------------------------------------------------
- 参考桌面 code/AGENTS.md 中的设计系统
- chatclow-web/CLAUDE.md → 新增完整设计规范章节（色彩/字体/圆角/阴影/动效/反模式）
- CLAUDE.md → 技术栈表格新增设计风格行

2026-06-06 05:00:00 全局 Handwritten Sketch 设计系统实施
---------------------------------------------------
【设计系统基础】
- index.html → 添加 Google Fonts（Kalam + Patrick Hand），lang 改为 zh-CN
- style.css → 完整重写为手绘风格：CSS 变量（纸张底色/墨水黑/标记笔红）、
  硬阴影预设、wobbly 圆角预设、Element Plus 全局覆盖（按钮/输入框/表格/弹窗等）

【Login.vue 手绘化】
- 删除：粒子特效 / 深色渐变背景 / 玻璃卡片 / 暗色输入框 / 渐变按钮
- 新增：暖白纸张背景 / wobbly 特性卡片 / 透明胶带装饰 / 标记笔红提交按钮 /
  波浪下划线链接

【Chat.vue 手绘化】
- 侧边栏：浅色 #f8f6f0 背景（替代深色渐变），3px 右边框
- Logo：标记笔红方块 + Kalam 字体
- 会话项：悬停 3px 边框，激活态标记笔红
- 工具栏：白色卡片 + 3px 底边框 + 透明胶带装饰
- 消息气泡：用户=红色+硬阴影，AI=白色卡片+硬阴影+wobbly 圆角
- 打字指示器：红色脉冲动画

【Knowledge.vue 手绘化】
- 页面背景改为 var(--bg-page)
- Header：白色卡片 + 3px 底边框 + 硬阴影
- 知识库卡片：3px 边框 + 硬阴影 + wobbly-1 圆角
- 图标方块：标记笔红 + 3px 边框
- 文档项：2px 边框 + 小硬阴影，悬停滑移
- 上传拖拽区：3px 边框 + 硬阴影

【Admin.vue 手绘化】
- 侧边栏：浅色 #f8f6f0 背景（替代深色渐变），3px 右边框
- 侧边栏标题：Kalam 字体，3px 分割线
- 菜单项：墨水黑文字，激活态左侧标记笔红边框
- 内容面板：3px 边框 + 硬阴影 + wobbly-1 圆角
- 智能体卡片：3px 边框 + 硬阴影 + wobbly-2 圆角
- 头像：3px 边框 + 硬阴影（移除圆形，改为方形）
- 详情面板：3px 边框 + 硬阴影 + wobbly 圆角
- Section 标题：波浪下划线替代蓝色左边框
- 聊天预览：3px 边框，用户气泡标记笔红
- 提供商卡片：3px 边框 + wobbly-4 圆角
- 模型表格区：3px 边框 + 硬阴影 + wobbly-2 圆角
- 对话详情：2px 边框 + 小硬阴影

【TokenStats.vue 手绘化】
- 页面背景改为 var(--bg-page)
- 统计卡片：3px 边框 + 硬阴影 + wobbly-2 圆角
- 数值颜色改为标记笔红

2026-06-06 05:30:00 对比度增强（黄色卡片 + 蓝色助手 + 黑色设置栏）
---------------------------------------------------------------
【Chat.vue 对比度调整】
- AI 消息气泡：从白色改为卡通便利贴黄色 #fef5d4，边框暖金色
- AI 头像：标记笔红 → 钢笔蓝（accent），突显助手身份
- 用户头像：钢笔蓝 → 标记笔红
- Agent 标签：info 灰色 → primary 蓝色
- Agent 选择区：浅蓝底 tint
- 侧边栏底部（知识库/用量统计/退出按钮）：纯黑背景 + 白色文字
  悬停变为标记笔红，加硬阴影 + 上浮动效
- 侧边栏改为更暖的米色 #f5f0e8，增强纸质感对比
- 会话列表（左侧）：改为黄色便签卡片效果
  默认：#fef9e7 浅黄底 + 金色边框 + 小硬阴影
  悬停：#fef5d4 中黄 + 硬阴影 + 右滑 2px
  激活：#fce88d 深黄 + 粗金边框 + 右滑 4px + 标题加粗手写体

2026-06-06 06:00:00 登录体验优化（自动新对话 + 快捷提问）
-------------------------------------------------------
【User.java】移除 role 字段的 @NotNull 校验
- 注册时前端不传 role，后端 register() 已默认 setRole(1)
- @Valid 提前拦截导致注册失败

【Chat.vue 用户引导优化】
- 登录后无会话时：左侧自动显示「新对话」黄色占位卡片（带"新"标签）
- loadConversations 自动调用 newConversation() 进入就绪状态
- 空白欢迎区改为：👋 表情 + "开始新对话" + 快捷提问建议词
- 快捷提问：点击自动填入问题并发送（解释 RAG / 写邮件 / AI Agent）

2026-06-06 06:30:00 智能体全局共享 + 注册问题修复
--------------------------------------------------------
【后端】AiAgentController.java
- listByUserId 改为所有用户都返回全部启用智能体（原普通用户只看到自己的）
- 移除 UserMapper 不再需要的注入

【前端】Chat.vue
- 非管理员隐藏"新建智能体"按钮和弹窗
- 所有用户都能看到并使用系统中已启用的智能体

2026-06-06 07:00:00 新用户引导弹窗：选择智能体飞行动画
--------------------------------------------------------
【Chat.vue 新用户引导】
- 新用户首次登录时（无会话 + 未完成引导），弹出半透明遮罩
- 中间展示所有可用智能体卡片（emoji + 名称 + 描述）
- 点击卡片触发飞行动画：卡片缩小 → 飞到左侧智能体选择区 → 自动选中
- 可选"暂时跳过"，之后不再次弹出（sessionStorage 标记）
- 非首次登录或已有会话的用户直接跳过引导，默认选中第一个智能体
- 引入 AGENT_EMOJI_MAP 根据智能体名称关键词匹配 emoji（☕🎨📖等）

【修复】User.java 移除 role 字段 @NotNull 校验
- 注册时前端不传 role，后端已默认 setRole(1)，但 @Valid 提前拦截导致"角色不能为空"报错


2026-06-06 14:50:00 知识库权限体系：用户可创建自己的知识库，管理员可管理全部
--------------------------------------------------------------------------
【需求】
- 所有用户都可以创建知识库
- 所有用户都可以查看全部知识库
- 只有管理员或知识库创建者才能修改/删除/上传文档
- 管理员可以管理所有知识库

【后端改动】
- RagKnowledgeBase.java → 新增 userId 字段 + getter/setter
- RagKnowledgeBaseController.java → add() 自动从 JWT 取 userId 赋给 KB；
  update/delete/toggleStatus 增加 canManage 权限检查（管理员或创建者）
- RagDocumentController.java → upload/delete 增加 canManageKb 权限检查
- schema-h2.sql → chatclow_rag_kb 表加 user_id 列

【前端改动】
- Knowledge.vue → "新建知识库"对所有用户开放
- Knowledge.vue → 操作下拉菜单改为 v-if="canManage(kb)"（管理员或创建者）
- Knowledge.vue → 文档删除按钮同理
- Knowledge.vue → 新增 canManage() 函数判断操作权限
- Knowledge.vue → 自己的知识库显示"我的"标签

2026-06-06 14:50:00 智能体权限体系：每人只能管理自己的智能体
--------------------------------------------------------------------------
【需求】
- 所有用户都可以创建智能体
- 每人只能看到自己创建的智能体（私有）
- 只有创建者或管理员可以修改/删除/切换状态
- 管理员可以在管理后台看到所有智能体

【后端改动】
- AiAgentController.java → add() 从 JWT 取 userId（不信任前端传入）；
  listByUserId() 改为按 userId 过滤（每人只看自己的）；
  update/delete/toggleStatus 增加 canManage 权限检查
- AiAgentServiceImpl.java → listByUserId 原实现已支持 userId 过滤（未改）

【前端改动】
- Chat.vue → 去掉所有 v-if="userStore.role === 2" 限制，
  普通用户也可创建和管理自己的智能体

2026-06-06 15:02:00 修复：管理员创建的智能体用户端不可见
---------------------------------------------------
【问题】listByUserId 只返回 userId 匹配的智能体，
管理员创建的智能体（userId ≠ 当前用户）普通用户看不到
【修复】AiAgentServiceImpl.java → listByUserId 改为返回
用户自己的 + 管理员创建的智能体（管理员 role=2 的用户视为公共智能体创建者）

2026-06-06 17:00:00 底部导航改为大卡片样式
---------------------------------------------------
【前端改动】
- Chat.vue → sidebar-footer 模板重写：
  - 去掉三个 el-button，改为三张 nav-card
  - "知识库"卡片：文件夹图标 + "管理 RAG 文档" 描述
  - "用量统计"卡片：图表图标 + "Token 消耗概览" 描述
  - "退出登录"卡片：开关图标 + "切换账号" 描述，悬停变红色
- Chat.vue → 删除旧的 sidebar-footer CSS，新增 nav-card 系列样式
  - 白色卡片 + 3px 边框 + 硬阴影，与 agent-card 风格统一
  - 每个卡片带右箭头，悬停箭头变红右移
  - 退出卡片悬停整张变标记笔红，文字变白

2026-06-06 17:25:00 修复使用帮助不显示（模板嵌套问题）
---------------------------------------------------
【问题】帮助引导 div 嵌套在 onboarding-overlay 内部，只有新用户引导时才显示
【修复】将 help-overlay 移到 onboarding-overlay 外部，作为 chat-layout 的平级兄弟节点

2026-06-06 17:20:00 使用帮助引导流程（三步）
---------------------------------------------------
【新增功能】
- Chat.vue → 点击"使用帮助"按钮触发三步引导：
  Step 1：智能体选择 → 点击卡片飞入侧边栏 → 自动进入下一步
  Step 2：RAG 知识库提示 → 卡片 1.5s 缓慢放大到正中间 → 展示 RAG 说明 + 箭头指向「知识库」
    → 点击"知道了"后 1.2s 缓慢缩小消失
  Step 3：对话选择 → 对话卡片逐个弹出（scale 0→1，带延迟）→ 可创建新对话/打开已有对话
    → 关闭引导
- 每步可跳过，引导结束重置状态
- CSS 动画：rag-zoom-in/out 缩放 + conv-pop-in 弹性弹出

2026-06-06 17:10:00 工具栏多彩闪烁使用帮助按钮
---------------------------------------------------
【前端改动】
- Chat.vue → toolbar-right 新增"使用帮助"卡片：
  五彩循环变色（红→橙→金→绿→蓝→紫），同色发光阴影
  悬停加速到 0.6s 一轮

2026-06-06 16:50:00 智能体管理面板优化：透明遮罩 + 分组显示
---------------------------------------------------
【修复】
- 遮罩变暗改为透明（保持点击关闭功能）
- 面板 z-index 提高到 5001 解决按钮被遮罩拦截的问题
【新增】
- 面板内智能体列表分为两组：
  管理员发布的 → "🛡️ 智慧助手"（agent.userId !== 当前用户）
  用户自己的 → "🎨 个性化助手"（agent.userId === 当前用户）
- 新增 computed: adminAgents / myAgents

2026-06-06 16:40:00 智能体 UI 重构：大卡片 + 右侧滑出管理面板
---------------------------------------------------
【前端改动】
- Chat.vue → 侧边栏智能体选择区重写：
  - 去掉 dropdown 下拉框和三个小按钮
  - 改为大卡片：emoji + 智能体名称 + "点击切换" 提示 + 右箭头
  - 卡片悬停有上浮动效 + 箭头变红右移
- Chat.vue → 新增右侧滑出管理面板（覆盖层动画）：
  - 头部："智能体管理"标题 + 关闭按钮
  - 当前智能体详情卡片：头像 + 名称 + 描述 + 编辑/删除按钮
  - 所有智能体列表：点击即可切换，当前使用中的打"使用中"标签
  - 底部："新建智能体"按钮
  - 滑入动画 0.3s ease-out
- Chat.vue → 新增 computed: currentAgent
- Chat.vue → 新增 selectAgentFromPanel() 切换智能体并关闭面板
- Chat.vue → 新增 ArrowRight 图标导入
- 删除旧的 watch(showAgentDialog) 和 handleCreateAgent 逻辑

2026-06-06 16:00:00 创建数据库链路可视化 HTML
---------------------------------------------------
【新增文件】
- chatclow-web/public/database-chain.html → 自包含 HTML，手绘风格
  展示 RAG 和 Tool 两大链路的数据库表关系、字段映射、shouldSkip 逻辑、
  当前实际数据、每个 Agent 的可用性分析、完整请求数据流时序

2026-06-06 18:00:00 帮助引导改进：Step 1 也飞入 + 减速动画
---------------------------------------------------------
【前端改动】
- Chat.vue → 模板：Step 1 智能体选择从静态 div 移入 help-trans-card，
  与 Step 2/3 共用放大缩小动画，从 agent-card 位置飞出
- Chat.vue → showHelp()：加 nextTick animateCardIn('.agent-card', 560)
- Chat.vue → advanceHelp()：Step 1 跳过改为调用 animateCardOut()
- Chat.vue → handleHelpBgClick()：所有 Step 统一点击背景缩回
- Chat.vue → helpSelectAgent()：删除 flyingCard 飞行动画，改为 animateCardOut() 缩回 agent-card
- Chat.vue → finishStep()：新增 Step 1→2 的处理（Step 2 从 nav-card-pink 飞出）
- Chat.vue → animateCardIn()：0.9s → 1.8s，timeout 950 → 1850
- Chat.vue → animateCardOut()：0.7s → 1.5s，timeout 750 → 1550，支持 Step 1 定位到 agent-card
- Chat.vue → CSS：删除 .help-step / .help-step-1 / @keyframes help-fade-in（不再需要）

2026-06-06 18:05:00 RAG 提示文字改为"点击空白处下一步"
---------------------------------------------------------
【前端改动】
- Chat.vue → Step 2 "← 前往知识库管理文档" → "点击空白处下一步 →"

2026-06-06 18:10:00 全局工具：所有智能体默认带上 get_current_time / get_weather / calculate
------------------------------------------------------------------------------
【需求】用户不会自己给 agent 配工具，基础工具应该每个 agent 默认就有
【后端改动】
- AiFunctionServiceImpl.java → listByAgentId 改为查 agent 自己的工具 + 全局工具（agent_id = -1）
【数据库】
- chatclow_ai_function 插入 3 条 agent_id = -1 的全局工具记录（get_current_time, get_weather, calculate）

2026-06-06 18:50:00 新增运营概览仪表盘（嵌入管理后台）
---------------------------------------------------------
【新增文件】
- chatclow-web/src/views/AdminDashboard.vue → 运营概览仪表盘组件
  - 4 项核心指标卡片：Token 消耗(紫)、总对话(靛蓝)、活跃用户(青蓝)、平均响应(绿)
  - 三大面板：记忆概览(含环形饼图SVG) / RAG 摘要(含进度条) / 技能仪表板(含调用量)
  - 右上角：近7天筛选 + 刷新按钮（含加载动画）
  - 全部使用手绘风格
【Admin.vue 改动】
- 侧边栏菜单新增「运营概览」选项（排第一位）
- 内容区新增 dashboard 面板，引入 AdminDashboard 组件

2026-06-06 19:40:00 前端性能优化 + 会话加载守卫
---------------------------------------------------
【性能优化】
- Chat.vue → startStreamRender：setInterval(30ms) → requestAnimationFrame
  RAF 与浏览器绘制同步，标签页后台时自动暂停，减少无用 CPU 消耗
- Chat.vue → scrollToBottom 仅在 streamText 内容真正变化时调用，
  避免每 30ms 无意义强制布局
【Bug 修复】
- Chat.vue → loadConversations 增加 userId 守卫
  （userId=0 时跳过请求，防止查出全量数据）

2026-06-06 19:30:00 仪表盘接入真实数据
-------------------------------------------
【前端改动】
- AdminDashboard.vue → 重写数据加载逻辑，调用现有后端 API：
  - Token 总消耗 → getAdminTokenSummary() 汇总所有用户 token
  - 总对话 → 遍历用户调用 /conversation/list 统计会话数
  - 活跃用户 → /user/admin/list 获取普通用户数
  - RAG 知识库数/文档数/分片数 → getKbList() + getDocList() 统计
  - 平均响应时间暂保持模拟数据（后端暂无响应耗时记录）
- 右上角刷新按钮真实刷新数据，加载态旋转动画

2026-06-06 20:30:00 平均响应时长追踪 — 后端全链路 + 前端仪表盘
--------------------------------------------------------------
【需求】每次 AI 响应记录耗时，在运营概览中展示真实平均响应时长
【后端改动】
- ChatContext.java → 新增 startTime / responseTime 字段 + getter/setter
- ModelCallStep.java → process() 开始记录 startTime，finally 中计算 responseTime
- SyncAiCallStep.java → process() 开始记录 startTime，finally 中计算 responseTime
- AgentConversationRecord.java → 新增 responseTime 字段（Long，毫秒）
- RecordSaveStep.java → 保存记录时写入 ctx.responseTime
- AgentConversationRecordMapper.java → 新增 @Select AVG(response_time) 查询
- TokenController.java → 新增 GET /api/admin/avg-response-time 端点
- schema-h2.sql → chatclow_ai_conversation_record 表加 response_time 列
【前端改动】
- api/token.js → 新增 getAdminAvgResponseTime() 接口
- AdminDashboard.vue → loadDashboardData 调用平均耗时接口，替换假数据
【数据库变更】
- ALTER TABLE chatclow_ai_conversation_record ADD COLUMN response_time BIGINT DEFAULT NULL;

2026-06-06 18:30:00 管理后台手绘化改造（Handwritten Sketch）
---------------------------------------------------------
【全局样式】
- style.css → 表格行 hover 改为暖黄色 #fef5d4（替代默认蓝色）
- style.css → 斑马纹行改为暖灰 #f8f6f0
- style.css → 新增 el-input-number wobbly 圆角
- style.css → 新增 el-select-dropdown-item 手写字体
【Admin.vue 模板装饰】
- 所有 .content-panel 添加透明胶带 ::before 装饰（rgba 半透旋转）
- .model-providers 添加透明胶带 ::before 装饰
【Admin.vue 文字样式】
- .panel-header h2 → 波浪下划线
- .provider-header h3 → 波浪下划线
- .model-table-header h3 → 波浪下划线
- .agent-detail-right h3 → 波浪下划线
- .sidebar-item → 手写字体
- .panel-subtitle → 手写字体
- .agent-card-footer → 手写字体
【Admin.vue 表单区域】
- .detail-section → 虚线边框 + 暖纸底色
- .form-row → 底部虚线分隔
- .form-row label → 标记笔字体
【Admin.vue 对话详情弹窗】
- .conv-detail-msg → wobbly 圆角 + 3px 边框

2026-06-06 21:00:00 用户管理表格列宽调整
-----------------------------------------
【最终列宽（用户管理表）】
- ID: 70px
- 用户名: 200px
- 邮箱: 720px
- 角色: 160px
- 剩余次数: 120px
- Token消耗: 189px
- 操作: 260px（按钮靠右排列，间距 8px）
【其他改动】
- 所有表格禁用列拖动（:resizable="false"）
- 单元格内边距收窄到 4px
- 管理后台内小按钮改为 padding: 4px 10px
- Panel 和 Element Plus 组件全部改为直角边框

2026-06-06 22:00:00 RAG 简化开关 — 去掉绑定知识库，开启则搜全部
-----------------------------------------------------------------
【需求】用户创建 agent 时不懂该绑定哪个知识库，改为只有开/关，开启后自动检索所有知识库
【后端改动】
- RagStep.java → shouldSkip() 去掉 kbId==null 条件；process() 中 kbId 为 null 时调 getDefaultVectorStore()
- VectorStoreFactory.java → 新增 getDefaultVectorStore() 方法，不依赖 kbId 返回可用向量存储实例
- VectorSearchServiceImpl.java → search() 和 keywordSearch() 中 kbId 为 null 时不加 WHERE kb_id 过滤
【前端改动】
- Admin.vue → 智能体详情和新建对话框中删除"绑定知识库"下拉框；保存时 kbId=null
- Chat.vue → 智能体表单中删除"知识库"选择框；保存时 kbId=null

2026-06-06 23:00:00 新增 4 个内置智能体（翻译/邮件/总结/找工作）

2026-06-06 23:30:00 新增"提示词工程师"智能体 + 智能体详情面板改系统提示词编辑器
-----------------------------------------------------------------
【新增智能体】
- 提示词工程师 — 从 qiuzhi-skill-creator 蒸馏出来的提示词生成助手
  引导用户通过 4 阶段交互（需求挖掘→结构设计→生成提示词→测试建议）
  输出结构化 Agent System Prompt
【前端改动】
- Admin.vue → 智能体详情右侧"预览与调试"改为"系统提示词"大面积编辑器
- Admin.vue → 删除左侧小面积系统提示词输入框，避免重复
- Admin.vue → 删除 testMessages/testInput/testLoading 等测试相关代码和 CSS
【数据库】
- chatclow_ai_agent 插入"提示词工程师"（user_id=1, model_id=1）
-----------------------------------------------------------------
【新增智能体】
1. 翻译助手 — 中英互译，支持各专业领域，保持原文格式和风格
2. 邮件助手 — 写商务邮件、回复客户，可选正式/亲切语气
3. 总结提炼助手 — 长文总结、会议纪要、要点提取、文档精炼
4. 找工作助手 — 分析简历并提出改进意见，根据岗位关键点修改简历，评估匹配度（含 STAR 原则重构）
【数据库】
- chatclow_ai_agent 插入 4 条记录，user_id=1（管理员），model_id=1，状态启用

2026-06-06 23:45:00 用户个人文档 RAG 系统（用户级知识库）
---------------------------------------------------------
【需求】用户不需要看到项目知识库，改为个人文档标签；用户上传文档后 agent 自动 RAG 检索
【新增文件】
- entity/UserDocument.java → 用户文档实体（id/userId/fileName/fileType/fileSize/filePath/content/chunkCount/status/errorMsg/createdDt）
- entity/UserChunk.java → 用户文档切片实体（id/userId/docId/chunkIndex/content/tokenCount/vectorData/contentHash/createdDt）
- mapper/UserDocumentMapper.java → MyBatis Plus 接口
- mapper/UserChunkMapper.java → MyBatis Plus 接口
- service/UserDocumentService.java → 接口：upload/listByUserId/delete/search
- service/impl/UserDocumentServiceImpl.java → 实现：上传→提取文本→切片→去重→向量化→入库，搜索（余弦相似度）
- controller/UserDocumentController.java → REST API（upload/list/delete/search/count）
- api/userDoc.js → 前端接口封装
【后端改动】
- RagStep.java → process() 末尾新增用户个人 RAG 检索（userId 隔离），结果以"【用户个人文档】"前缀拼入 ctx.ragContext
- schema-h2.sql → 新增 chatclow_user_document / chatclow_user_chunk 建表语句
【前端改动】
- Chat.vue → 导航卡片"知识库"改为"个人文档"，点击打开内嵌面板（上传拖拽区 + 文档列表 + 删除）
- Chat.vue → 删除 Knowledge.vue 嵌入引用，activePanel 值改为 'user-docs'
【数据库变更】
- CREATE TABLE chatclow_user_document（用户文档主表）
- CREATE TABLE chatclow_user_chunk（文档切片表，含向量数据）

2026-06-06 23:50:00 上传文档后状态轮询 + 点击文档跳回聊天
---------------------------------------------------------
【修复】上传文档后一直显示"解析中"不刷新 → 新增轮询机制
- doUpload 上传成功后启动定时器（每2秒刷新文档列表）
- 所有文档处理完成（status 3或4）自动停止轮询
- 离开个人文档面板时自动停止轮询
【修复】点击左侧会话列表不跳转回聊天
- switchConversation 新增 activePanel.value = 'chat'
【修复】Vue 警告 ArrowLeft 未定义
- Chat.vue 图标导入列表新增 ArrowLeft

2026-06-07 02:00:00 多项修复与功能改进
-----------------------------------------
【修复】异步处理文档事务问题
- UserDocumentServiceImpl.java → 使用 TransactionSynchronizationManager.afterCommit()
  确保事务提交后再启动异步线程处理切片，修复上传后一直"解析中"的问题
【修复】智能体详情页移除右上角编辑按钮
- Admin.vue → agent-detail-actions 删除编辑按钮
【修复】用量统计、个人文档嵌入面板移除返回聊天按钮
- Chat.vue → token-stats、user-docs 面板删除返回按钮
- TokenStats.vue → 组件内部返回按钮删除并清理 ArrowLeft 导入
【新增】智能体头像支持图片
- Admin.vue → avatarStyle() 支持 agent.avatar 字段，有头像时显示 background-image
- Chat.vue → 智能体卡片和管理面板支持显示头像图片
- 数据库 chatclow_ai_agent.avatar 设置 6 个智能体头像
- chatclow-web/public/avatars/ 新增 6 个头像图片
【新增】聊天对话 AI/用户头像替换为图片
- Chat.vue → 消息区 AI 头像显示 /avatars/对话头像.jpg
- Chat.vue → 消息区用户头像显示 /avatars/对话头像本人.jpg
- chatclow-web/public/avatars/ 新增 对话头像.jpg、对话头像本人.jpg
【优化】使用帮助引导动画改为飞行动画
- animateCardIn/Out 从 1.8s 缩放改为 0.5s 飞行（与新手引导一致）
- 去除 height:auto 不能做 CSS transition 的问题

2026-06-07 02:28:00 大模型新增"测试连接"按钮
-----------------------------------------
【需求】管理员新增或编辑模型时，可以点击"测试连接"验证 API 是否可用
【后端改动】
- AiModelController.java → 新增 POST /api/model/test 端点
- AiModelService.java → 接口新增 testConnection() 方法
- AiModelServiceImpl.java → 实现 testConnection()：用 OkHttp 发送
  { model, messages:["Hi"], max_tokens:5, stream:false } 验证 API 响应
【前端改动】
- api/model.js → 新增 testModelConnection() 接口
- Admin.vue → 模型对话框底部新增"测试连接"按钮（带 loading 状态）

2026-06-07 02:30:00 智能体绑定模型对接大模型管理
-----------------------------------------
【改动】
- Admin.vue → 智能体详情页"绑定模型"从硬编码（DeepSeek-V3/Qwen-Max/GPT-4o）
  改为 v-for 遍历 modelList 动态渲染
- Admin.vue → 新建/编辑智能体对话框"绑定模型"从 el-input-number 手动输入 ID
  改为 el-select 下拉选择，与已有模型数据对接
- Admin.vue → openAddAgentDialog 默认取第一个可用模型 ID

2026-06-07 02:30:00 修复智能体绑定不存在的模型导致对话报错
-----------------------------------------------
【问题】管理员删除模型后重新添加，自增 ID 变化导致智能体引用不存在的 modelId
【修复数据】chatclow_ai_agent 中 model_id=1 的记录更新为 3
【后端加固】ContextAssemblyStep → 加载模型时如果绑定的模型不存在，
自动取第一个启用模型兜底，避免直接抛异常
【日志】2026-06-07 02:30:00 修复智能体绑定模型不存在问题

2026-06-07 03:35:00 修复工具名拼接 bug（get_current_timenull）
-----------------------------------------------
【问题】小米 MiMo API 流式返回 tool_calls 时，function.name 的分片 chunk
中 name 字段为 null，Jackson 的 asText() 在 NullNode 上返回字符串 "null"，
拼接后变成 get_current_time + null = "get_current_timenull"
【修复】ModelCallStep.java → fn.has("name") + isEmpty() 改为
fn.path("name").isTextual()，只有真正字符串值才拼接
【数据库】chatclow_ai_conversation 新增 model_id BIGINT, model_name VARCHAR(100)
【前端】Chat.vue → 会话列表每条显示模型名称（红色小字）
【数据库】chatclow_ai_conversation 新增 agent_id BIGINT

2026-06-07 03:40:00 切换会话时自动切换对应的智能体
-----------------------------------------------
【需求】点击会话列表中的对话时，自动切换到当时使用的智能体
【后端】ContextAssemblyStep → 新建会话时保存 agentId
【数据库】chatclow_ai_conversation 新增 agent_id 列
【前端】Chat.vue → switchConversation 中根据 conv.agentId 自动切换 currentAgentId

2026-06-07 03:45:00 新对话按钮跳转到对话面板
-------------------------------------------
【改动】Chat.vue → newConversation() 新增 activePanel.value = 'chat'

2026-06-07 03:50:00 使用帮助引导动画优化（减速 + 防闪烁 + 来源修正）
---------------------------------------------------------
【改动清单】
1. 动画减速：飞入 0.5s→1s，飞回 0.4s→0.8s
2. 防闪烁：showHelp() 先定位卡片再显示遮罩，避免遮罩单独出现
3. 遮罩淡入：help-overlay 新增 0.3s ease 淡入动画
4. 步进时卡片不隐藏：animateCardOut 在步进时不再隐藏卡片，直接切换内容重飞
5. 动画来源修正：
   - Step 1（智能体）：从 agentCardRef（模板 ref）定位，避免 querySelector 偏差
   - Step 2（个人文档）：从 .nav-card-pink 定位 ✓
   - Step 3（对话消息）：从 .conv-item 定位（原为加号按钮）
6. 代码清理：showHelp 不再依赖 nextTick + animateCardIn，直接内联动画

2026-06-07 Chat.vue 前端代码重构
------------------------------

【重构内容】
1. 模板 `v-for="m in models"` → `v-for="m in modelList"`（消除未声明变量引用）
2. 删除重复声明的 `agentForm` / `agentRules`（声明了两次，第二次覆盖第一次）
3. 移除冗余的 `loadModels()` 函数，统一使用 `loadModelList()`
4. `showHelp()` 改用 `animateCardIn('.agent-card', 560)` 消除代码重复

【改动文件】
- chatclow-web/src/views/Chat.vue

2026-06-07 使用帮助新增第4步：Token用量查询
----------------------------------------

【新增】
- Step 4：用量统计引导，从 .nav-card-blue（用量统计卡片）飞入/飞回
- finishStep() 增加 step 3→4 过渡，step 4 为最后一步关闭引导

【修复】
- animateCardOut 飞回位置修正：两帧法锁定状态、agentCardRef 兜底 querySelector、
  增加 height/padding:0 让缩放更精准
- animateCardIn 支持传 DOM 元素或 CSS 选择器（showHelp 改用 agentCardRef）

【改动文件】
- chatclow-web/src/views/Chat.vue

2026-06-07 04:30:00 对话上传文件/图片功能（完整实现）
------------------------------------------------------
【需求】用户可以在对话中上传文件（尤其是图片），让 AI 直接分析内容，比如"分析这张图片"
不走 RAG 知识库，是对话级别的文件上传

【后端改动】
- UserDocument.java → 新增 source（rag/chat）、conversationId 字段
- ChatRequest.java → 新增 fileIds 字段（List<Long>）
- ChatContext.java → requestMessages 类型 List<Map<String,String>> → List<Map<String,Object>>
- ChatContext.java → 新增 chatFiles、fileIds 字段
- ChatService.java → chat/chatStream 新增 fileIds 参数
- ChatServiceImpl.java → 传递 fileIds 到 ChatContext
- ChatController.java → 新增 POST /api/chat/upload 文件上传端点
- WebMvcConfig.java → addResourceHandlers 映射 /uploads/**
- ContextAssemblyStep.java → 有 fileIds 时查询 UserDocumentMapper
- MessageBuildStep.java → 支持 content 数组格式（图片 base64 + 文字 + 文件）
- SyncAiCallStep.java → List<Map<String,String>> → List<Map<String,Object>>
- ToolCallHelper.java → List<Map<String,String>> → List<Map<String,Object>>
- ModelCallStep.java → 无类型变化（已使用 Object）
- schema-h2.sql → chatclow_user_document 加 source、conversation_id 列

【前端改动】
- api/chat.js → chatStream 传 fileIds 参数；新增 uploadChatFile() 接口
- Chat.vue → 输入框旁加 Paperclip 上传按钮 + 文件预览缩略图（支持图片预览）
- Chat.vue → 发送消息时携带 fileIds，发送后自动清除已上传文件
- Chat.vue → 切换智能体/新建会话时清除文件列表
- Chat.vue → CSS：.input-row / .chat-file-preview / .chat-file-item / .chat-file-thumb

【测试修复】
- ChatServiceImplTest.java → chat/chatStream 加 null fileIds 参数
- ChatControllerTest.java → mock 加 any() fileIds 参数
- ToolCallHelperTest.java → Map<String,String> → Map<String,Object>
- MessageBuildStepTest.java → 类型适配 + String 转型

2026-06-07 04:40:00 修复 MySQL 缺少 source 字段错误
-----------------------------------------------
【问题】上传文件时报 Unknown column 'source'，因为 MySQL 表结构没有更新
【修复】ALTER TABLE chatclow_user_document ADD COLUMN source VARCHAR(20) DEFAULT 'rag'
       ALTER TABLE chatclow_user_document ADD COLUMN conversation_id BIGINT

2026-06-07 修复管理员用户管理操作列溢出
------------------------------------------
【问题】操作栏按钮超出表格边界
【改动】
- Admin.vue → 邮箱列 720px→260px，角色列 160px→120px，剩余次数 120px→100px，Token消耗 189px→150px
- Admin.vue → 操作列 260px→340px，按钮 padding: 5px 10px，flex-shrink:0 防止挤压

2026-06-07 管理员用户管理列宽最终调整
------------------------------------------
【改动】
- Admin.vue → 操作列 260px→520px（扩大一倍），表头居中，按钮居中
- Admin.vue → 邮箱列 260px→520px（扩大一倍）
- Admin.vue → 去掉多余的表格宽度 calc 和内边距覆盖，列直接贴表格右边框

2026-06-07 04:50:00 修复 DeepSeek 多模态格式 + 消息气泡显示图片
---------------------------------------------------------------
【踩坑记录】DeepSeek V4 Flash API 不支持图片识别！
- 查文档说 DeepSeek V4 Flash 有多模态（284B/13B 模型已开源）
- 但实际上 API 层面没开放，只有官方 App/Web 端的"识图模式"能用
- 发 image_data 字段不会报错，但模型直接忽略图片内容
- 小米 MiMo 同样不支持（404）
- 结论：DeepSeek/MiMo 当前都是纯文本模型，不用折腾多模态了

【后端改动】
- MessageBuildStep.java → 去掉所有 base64 图片处理代码
  图片上传后仅在消息文本中标记"[用户上传了图片: xxx.jpg]"
  txt/md 文件正常传内容，其他文件仅标记文件名

【前端改动】
- Chat.vue → sendMessage 保存文件信息到 userMsg.files
- Chat.vue → 模板消息气泡显示图片缩略图/文件名
- Chat.vue → CSS：.msg-files / .msg-file / .msg-file-img

2026-06-07 05:00:00 新增系统公告功能（悬浮公告按钮 + 管理员管理）
-----------------------------------------------------------------
【需求】用户页面顶部居中悬浮公告按钮，点击展开公告内容；管理员可增删改公告
【后端新增】
- entity/Announcement.java → 公告实体（id/title/content/status/createdDt/updatedDt）
- mapper/AnnouncementMapper.java → MyBatis Plus 接口
- service/AnnouncementService.java → 接口：add/listAll/getLatestEnabled/update/deleteById
- service/impl/AnnouncementServiceImpl.java → 实现
- controller/AnnouncementController.java → REST API（GET /latest、admin CRUD）
- schema-h2.sql → 新增 chatclow_announcement 建表
【前端新增】
- api/announcement.js → 接口封装
【前端改动】
- Chat.vue → toolbar 新增居中公告按钮（Notification 图标 + "公告"文字）
- Chat.vue → 点击按钮展开悬浮公告弹窗（标题 + 内容 + 关闭按钮）
- Chat.vue → 有公告时加载最新一条展示，onMounted 自动获取
- Admin.vue → 侧边栏新增"公告管理"卡片（紫色 #f0abfc）
- Admin.vue → 公告列表展示（标题/内容预览/状态/时间 + 编辑/删除按钮）
- Admin.vue → 新增/编辑公告弹窗（标题 + 内容 + 启用/禁用开关）
【数据库变更】
- CREATE TABLE chatclow_announcement（系统公告表）

2026-06-07 13:10:00 修复删除文档未清理向量存储（PG残留孤立数据）
-----------------------------------------------------------------
【问题】前端删除文档后，MySQL chatclow_rag_document 记录已删，但
PostgreSQL rag_vectors 中的向量数据未清理，留下孤立数据
【根因】RagDocumentServiceImpl.deleteById() 只删文档记录，
没有调用向量存储的 deleteByDocument() 方法
【修复】RagDocumentServiceImpl.java → deleteById() 新增三步：
  1. 先查文档获取 kbId
  2. 调用 ragChunkService.deleteByDocumentId() 清理 MySQL 切片
  3. 通过 VectorStoreFactory 获取向量存储实例，
     调用 deleteByDocument() 清理 PG / MongoDB 向量
【数据清理】docker exec chatclow-pg → DELETE FROM rag_vectors WHERE doc_id = 24
  清理了 8 条孤立向量（对应前端已删的"张文亮(1).pdf"）

2026-06-07 13:30:00 修复三大删除未级联问题（知识库/模型/智能体）
-----------------------------------------------------------------
【问题1】删除知识库 → 文档和向量数据残留
- RagKnowledgeBaseServiceImpl.deleteById() 只删 KB 记录
- 重建同名 KB 后上传文档，existsByHash 查到旧向量跳过入库
【修复1】RagKnowledgeBaseServiceImpl.java → deleteById() 新增：
  1. 查出 KB 下所有文档
  2. 逐个调用 ragDocumentService.deleteById() 级联清理向量
  3. 最后删 KB 记录

【问题2】删除大模型 → 智能体引用已失效的 modelId
- AiModelServiceImpl.deleteById() 只删模型记录
- 重新添加模型后自增 ID 变了，旧 modelId 指向错误模型
- ContextAssemblyStep 有兜底，但会报错再恢复，体验差
【修复2】AiModelServiceImpl.java → deleteById() 新增：
  1. 自动找一个其他已启用的模型作为替补
  2. 将所有引用被删模型的 agent 的 modelId 更新为替补模型
  3. 最后删模型记录（@Transactional）

【问题3】删除智能体 → 关联会话和工具函数残留
- AiAgentServiceImpl.deleteById() 只删 agent 记录
- 历史会话的 agentId 指向已删除的 agent，前端显示空白
- 自定义工具函数孤立
【修复3】AiAgentServiceImpl.java → deleteById() 新增：
  1. 查出所有关联会话，逐个调用 deleteConversation() 级联删聊天记录
  2. 删除该智能体的自定义工具（保留全局工具 agentId=-1）
  3. 最后删智能体记录（@Transactional）

【改动文件】
- RagKnowledgeBaseServiceImpl.java
- AiModelServiceImpl.java
- AiAgentServiceImpl.java

2026-06-07 14:20:00 删除用户"剩余次数"字段（无用功能）
---------------------------------------------------
【需求】用户剩余次数属性无用，全链路删除
【后端改动】
- User.java → 删除 totals 字段
- UserServiceImpl.java → 删除 user.setTotals(100)
【前端改动】
- Admin.vue → 删除用户表格"剩余次数"列
- Admin.vue → 删除用户编辑表单"剩余次数"表单项
- Admin.vue → userForm 初始化去掉 totals: 100
【测试改动】
- schema-h2.sql → 删除 totals 列定义
【数据库变更】
- ALTER TABLE chatclow_ai_user DROP COLUMN totals;

2026-06-07 15:00:00 修复对话卡死不报错问题
---------------------------------------------------
【问题】责任链中 Step 抛异常时，DONE 事件不发，
前端一直等待导致界面卡死，无任何提示
【修复】ChatServiceImpl.java → catch 块先发 SseEvent.error()
再关闭连接，前端展示错误信息

2026-06-07 15:10:00 新增对话终止按钮（手动停止AI回复）
---------------------------------------------------
【需求】AI回复卡死时用户可手动终止
【前端改动】
- Chat.vue → 输入框发送按钮改为：streaming 时显示红色停止按钮，
  非 streaming 时显示正常发送按钮
- Chat.vue → 新增 stopStream() 函数，通过 AbortController 中断请求
- Chat.vue → sendMessage() 创建 AbortController 传给 chatStream
- Chat.vue → catch 块区分 AbortError（显示"已终止"）和其他错误
- chat.js → chatStream 新增 signal 参数传入 fetch

2026-06-07 15:30:00 替换 new Thread() 为线程池 sseExecutor
---------------------------------------------------
【问题】文档上传异步处理用 new Thread()，高并发下可能 OOM
【修复】
- RagDocumentServiceImpl.java → new Thread() 改为 sseExecutor.execute()
- UserDocumentServiceImpl.java → new Thread() 改为 sseExecutor.execute()
【线程池配置】
- corePoolSize=5, maxPoolSize=20, queueCapacity=100

2026-06-07 16:00:00 全量替换 System.out/err 为 SLF4J 日志
---------------------------------------------------
【改动】9 个文件，约 35 处 System.out/err → log.info/warn
- ChatChainImpl.java → 新增 Logger，2 处替换
- RagStep.java → 新增 Logger，1 处替换
- MongoDBVectorStore.java → 新增 Logger，5 处替换
- VectorStoreFactory.java → 新增 Logger，5 处替换
- EmbeddingServiceImpl.java → 新增 Logger，3 处替换
- RerankServiceImpl.java → 新增 Logger，8 处替换
- UserDocumentServiceImpl.java → 新增 Logger，2 处替换
- VectorSearchServiceImpl.java → 新增 Logger，7 处替换
- RagDocumentServiceImpl.java → 已有 Logger，13 处替换

2026-06-07 16:30:00 API 密钥加密存储（AES-128）
---------------------------------------------------
【新增文件】
- util/AesUtil.java → AES 加密/解密/脱敏工具类
【后端改动】
- application.yml → 新增 crypto.key 配置项
- AsyncConfig.java → @PostConstruct 初始化 AesUtil 密钥
- AiModelServiceImpl.java → add 加密 / update 检测脱敏值 /
  list/getById 返回脱敏 / testConnection 自动解密
- ContextAssemblyStep.java → 加载模型时解密 apiKey
  （旧明文密钥兼容：解密失败自动使用原值）

2026-06-07 17:00:00 SSE 断线重连 — 重新发送按钮
---------------------------------------------------
【问题】网络波动导致 SSE 连接断开，对话中断无恢复手段
【前端改动】
- Chat.vue → catch 块非终止错误时保存 retryText
- Chat.vue → 输入区上方显示黄色重试横幅 + "重新发送"按钮
- Chat.vue → retryLastMessage() 清除空白 AI 回复，填入原文重发
- Chat.vue → 正常完成/done 时清空 retryText
【效果】
- 网络波动断了 → 显示黄色横幅 → 点"重新发送" → 重新请求 AI
