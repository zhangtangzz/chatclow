==================== ChatClow 快速部署 ====================
场景：前端代码改动后需要更新服务器

# 步骤（3 步搞定）
# 1. 把改动的源文件上传到服务器覆盖
#    例如：scp Login.vue root@121.41.95.3:/www/wwwroot/chatclow/chatclow-web/src/views/
#    或者直接通过宝塔面板上传

# 2. SSH 登录服务器编译
ssh root@121.41.95.3 "cd /www/wwwroot/chatclow/chatclow-web && npm run build"

# 3. 刷新浏览器验证
#    完事！不用传 dist，不用重启后端
================================================================
